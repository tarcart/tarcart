{
  "name": "tarcart-api",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",

  "engines": {
    "node": "22.x",
    "npm": "10.x"
  },

  "scripts": {
    "start": "node dist/index.js",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "postbuild": "mkdir -p dist/public && cp -R public/* dist/public/"
  },

  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  },

  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^18.17.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
